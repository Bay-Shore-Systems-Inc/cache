{
    "name": "cache",
    "version": "0.6.7",
    "scripts": {
        "prepare": "husky install",
        "check": "go vet -v ./... && go test -v -cover ./... && gosec ./..."
    },
    "devDependencies": {
        "@commitlint/cli": "^19.8.0",
        "@commitlint/config-conventional": "^19.8.0",
        "husky": "^8.0.0"
    }
}
